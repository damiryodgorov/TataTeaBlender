declare class Pool<T> {
    private pool;
    private Func;
    constructor(Func: Pool.Resettable<T>);
    get(): T;
    release(obj: T): void;
}
declare module Pool {
    interface Resettable<T extends Object> {
        new (): T;
        reset?(obj: T): void;
    }
}
export = Pool;
